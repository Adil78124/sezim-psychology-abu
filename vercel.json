{
  "buildCommand": "chmod +x build.sh && ./build.sh",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "rm -rf node_modules package-lock.json && npm cache clean --force && npm install --no-optional",
  "devCommand": "npm run dev"
}