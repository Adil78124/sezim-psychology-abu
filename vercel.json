{
  "buildCommand": "chmod +x build.sh && ./build.sh",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "rm -rf node_modules package-lock.json && npm cache clean --force && npm install --include=optional",
  "devCommand": "npm run dev"
}