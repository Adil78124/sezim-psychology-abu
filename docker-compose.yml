services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: psychology-backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development
      - PORT=5000
      - FRONTEND_URL=http://localhost:3000
      - SUPABASE_URL=https://mzmouzcbmyhktvowrztm.supabase.co
      - SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im16bW91emNibXloa3R2b3dyenRtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjExMjk5ODQsImV4cCI6MjA3NjcwNTk4NH0.0DmDT1qiHdB8BpdJGRaGFBGQRgQ3HxZISNYHwp_s8iw
      - TELEGRAM_BOT_TOKEN=7982241397:AAGzinVEu6w_BgUrTOy2PPyEtyfssMVKJvU
      - TELEGRAM_CHAT_ID=-1002854667099
      - ADMIN_API_KEY=sezim-psychology-secret-2025
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_SECURE=false
      - SMTP_USER=kairatovadil7@gmail.com
      - SMTP_PASS=qlgqiqjvoipfmzbp
      - EMAIL_FROM=kairatovadil7@gmail.com
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev"
    networks:
      - psychology-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "sh", "-c", "curl -f http://localhost:5000/api/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: psychology-frontend
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_SUPABASE_URL=https://mzmouzcbmyhktvowrztm.supabase.co
      - REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im16bW91emNibXloa3R2b3dyenRtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjExMjk5ODQsImV4cCI6MjA3NjcwNTk4NH0.0DmDT1qiHdB8BpdJGRaGFBGQRgQ3HxZISNYHwp_s8iw
      - REACT_APP_API_URL=http://localhost:5000
      - HOST=0.0.0.0
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - FAST_REFRESH=false
      - CI=false
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - psychology-network
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

networks:
  psychology-network:
    driver: bridge

